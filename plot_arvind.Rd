% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{plot_arvind}
\alias{plot_arvind}
\title{Diagnostic Plots for Arvind Models}
\usage{
plot_arvind(fit, output_dir = tempdir(), prefix = NULL)
}
\arguments{
\item{fit}{an object of class \code{"ArvindFit"}.}

\item{output_dir}{character; directory where plots are saved. Defaults to
a temporary directory.}

\item{prefix}{character or \code{NULL}; prefix for plot filenames. If
\code{NULL}, derived from the model type.}
}
\value{
The \code{fit} object is returned invisibly.
}
\description{
Generates up to 25 diagnostic plots for a fitted \code{ArvindFit} object,
including observed vs fitted, residual histogram with Arvind density overlay,
Q-Q plot, ACF, ECDF comparison, and more.
}
\examples{
\donttest{
dat <- simulate_arvind_data(n = 50, seed = 1)
m1 <- fit_rw1(Y ~ X1 + X2 + X3, dat, seed = 42)
plot_arvind(m1, output_dir = tempdir())
}

}
